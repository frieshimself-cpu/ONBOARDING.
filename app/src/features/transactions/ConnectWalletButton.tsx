import { WalletMultiButton } from '@solana/wallet-adapter-react-ui'

/** Themed wrapper around wallet-adapter's connect/disconnect button. */
export function ConnectWalletButton() {
  return <WalletMultiButton />
}
