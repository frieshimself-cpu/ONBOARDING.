[toolchain]

[features]
resolution = true
skip-lint = false

# ⚠️ REPLACE this program id: after `anchor build`, run `anchor keys sync`
# (or `anchor keys list`) and paste the generated id here AND in
# programs/onboarding_fee/src/lib.rs (declare_id!) AND in the app's
# VITE_FEE_PROGRAM_ID. See README "Values you must paste".
[programs.localnet]
onboarding_fee = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[programs.devnet]
onboarding_fee = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
